## ModLoader Testing 3-2-2022 Notes

Main Window:

* File menu -> Import Mods, you cannot select multiple files. FIX THIS
* Packs based on other packs, mergers are not properly distinctly marked as fallback mergers in Change List

Pack Updater Window:

* Fallback select: You can make a mod dependent on itself >(
* Updates on unpacked mods should be written to the folder as metadata files instead of to the zip