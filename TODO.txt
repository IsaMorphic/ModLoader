Mod maker:
Make diff creator/editor (maybe... since text files are easier to create so it might not be necessary)

Questions from Polly:

Is there a way to automatically build the file system? <- yes there should be,
create a system wide game manager that allows you to quick launch the tool for
active game installations and to add new games and install the tool into them.

And maybe a bar at the top with an option to open the mods folder and
another to import? (as in it opens up a file window for you to search
for a file and you select the mod and it puts it into the mods
folder and reloads?)
^ do this as well

See .\Polly's Notes\ for more

Bug: FTP filesystem fails when trying to load a file into a directory that does not exist
